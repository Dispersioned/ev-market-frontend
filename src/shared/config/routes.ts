export const ROUTES = {
  admin: '/admin',
  auth: '/auth',
  cart: '/cart',
  catalog: '/catalog',
  checkout: '/checkout',
  compare: '/compare',
  home: '/',
  order: '/order/:id',
  profile: '/profile',
} as const;
